etcd-s3: true
etcd-s3-endpoint: "{endpoint}" # without https://
etcd-s3-bucket: nathanv-private
# R2 uses `auto` or `us-east-1` as regions
# B2 uses `us-east` as region
etcd-s3-region: auto
etcd-s3-access-key: "{accesskey}"
etcd-s3-secret-key: "{secretkey}"
etcd-s3-folder: etcd
kube-apiserver-arg:
  - oidc-issuer-url=https://authentik.nathanv.app/application/o/headlamp/
  - oidc-client-id={clientid}
  - oidc-username-claim=email
