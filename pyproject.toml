[project]
    name            = "homelab-new"
    version         = "0.1.0"
    description     = ""
    readme          = "README.md"
    requires-python = ">=3.13"
    dependencies    = ["pyinfra>=3.1.1", "pyyaml>=6.0.2"]

[dependency-groups]
    dev = ["pre-commit>=4.0.1"]
