apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  namespace: portainer
resources:
  - release.yaml
configMapGenerator:
  - name: portainer-values
    files:
      - values.yaml=values.yaml
