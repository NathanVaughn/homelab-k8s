apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-dns-pvc-postgresql
  namespace: technitium-dns
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
