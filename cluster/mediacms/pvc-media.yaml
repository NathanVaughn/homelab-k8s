apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mediacms-pvc-media
  namespace: mediacms
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
