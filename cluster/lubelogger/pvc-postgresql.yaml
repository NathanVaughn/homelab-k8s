apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lubelogger-pvc-postgresql
  namespace: lubelogger
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
