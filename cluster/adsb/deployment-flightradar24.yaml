apiVersion: apps/v1
kind: Deployment
metadata:
  name: adsb-flightradar24-deployment
  namespace: adsb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flightradar24
  template:
    metadata:
      labels:
        app: flightradar24
    spec:
      nodeSelector:
        role: apps
      containers:
        - env:
            - name: RECEIVER_TYPE
              value: relay
            - name: BEASTHOST
              value: readsb-data-service.adsb.svc.cluster.local
            - name: BEASTPORT
              value: "30005"
            - name: MLAT
              value: "yes"
          envFrom:
            - secretRef:
                name: adsb-env
          # registry-proxy image-prefix
          image: cr.nathanv.app/ghcr.io/sdr-enthusiasts/docker-flightradar24@sha256:d4d1a259e42bec1ea50f2f468d506ed43eda737da608b2fa18ad7416f7bf9051
          name: flightradar24
          volumeMounts:
            - mountPath: /run
              name: flightradar24-run-vol
      volumes:
        - name: flightradar24-run-vol
          emptyDir:
            medium: Memory
            sizeLimit: 64M
