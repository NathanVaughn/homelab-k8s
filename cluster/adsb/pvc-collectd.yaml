apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adsb-pvc-readsb-collectd
  namespace: adsb
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
