apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adsb-pvc-readsb-rrd
  namespace: adsb
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi
