apiVersion: v1
kind: Service
metadata:
  name: chrony-service
  namespace: chrony
  annotations:
    k8s-dns.nathanv.me/hostname: time.nathanv.app
    k8s-dns.nathanv.me/internal: ntp.svc.nathanv.home
spec:
  type: LoadBalancer
  loadBalancerIP: "10.0.1.3"
  selector:
    app: chrony
  ports:
    - port: 123
      protocol: UDP
