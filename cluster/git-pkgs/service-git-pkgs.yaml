apiVersion: v1
kind: Service
metadata:
  name: git-pkgs-service
  namespace: git-pkgs
  labels:
    app: git-pkgs
spec:
  type: ClusterIP
  selector:
    app: git-pkgs
  ports:
    - port: 8080
      protocol: TCP
