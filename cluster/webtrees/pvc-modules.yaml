apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: webtrees-pvc-modules
    namespace: webtrees
spec:
    storageClassName: longhorn
    accessModes:
      - ReadWriteOnce
    resources:
        requests:
            storage: 100Mi
