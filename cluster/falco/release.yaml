apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: falco-release
  namespace: falco
spec:
  releaseName: falco
  chart:
    spec:
      chart: falco
      sourceRef:
        kind: HelmRepository
        name: falco-repo
        namespace: falco
      version: "9.1.0"
  interval: 1h0m0s
  values:
    # registry-proxy block-start
    image:
      registry: cr.nathanv.app/docker.io
    driver:
      loader:
        initContainer:
          image:
            registry: cr.nathanv.app/docker.io
    falcoctl:
      image:
        registry: cr.nathanv.app/docker.io
    # registry-proxy block-end
    containerSecurityContext:
      privileged: true
      runAsUser: 0
      runAsGroup: 0
    nodeSelector:
      role: apps
    # enable alertmanager
    falcosidekick:
      enabled: true
      # registry-proxy block-start
      image:
        registry: cr.nathanv.app/docker.io
      # registry-proxy block-end
      config:
        alertmanager:
          hostport: http://prometheus-kube-prometheus-alertmanager.prometheus.svc.cluster.local:9093
          minimumpriority: "warning"
