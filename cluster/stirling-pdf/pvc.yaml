apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: stirling-pdf-pvc
    namespace: stirling-pdf
spec:
    storageClassName: longhorn
    accessModes:
      - ReadWriteOnce
    resources:
        requests:
            storage: 5Gi
