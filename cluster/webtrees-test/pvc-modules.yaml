apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webtrees-test-pvc-modules
  namespace: webtrees-test
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
